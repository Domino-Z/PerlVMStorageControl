<!DOCTYPE html>
<html>
<head>
    <title>Virtual Machines and Storage</title>
</head>
<body>
    <h1>Storage List</h1>
    <ul>
        [% FOREACH storage IN storage_list %]
        <li>[% storage.name %] - Capacity: [% storage.capacity %]</li>
        [% END %]
    </ul>

    <h1>Virtual Machine List</h1>
    <ul>
        [% FOREACH vm IN vm_list %]
        <li>[% vm.name %] - OS: [% vm.os %] - Storage ID: [% vm.storage_id %]</li>
        [% END %]
    </ul>
</body>
</html>